@model TrainingManagementSystem.Models.Entities.Organizition

@{
    ViewData["Title"] = "تأكيد الحذف";
}

<div class="alert alert-danger text-center mt-4" role="alert">
    <h4 class="alert-heading"><i class="fas fa-exclamation-triangle me-2"></i>تحذير!</h4>
    <p>هل أنت متأكد أنك تريد حذف هذه الجهة بشكل نهائي؟ لا يمكن التراجع عن هذا الإجراء.</p>
</div>

<div class="card shadow-lg border-danger">
    <div class="card-header bg-danger text-white">
        <h3>بيانات الجهة المراد حذفها</h3>
    </div>
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-3">اسم الجهة</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.Name)</dd>
            <dt class="col-sm-3">اسم المسؤول</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.ChiefName)</dd>
        </dl>

        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <div class="d-flex justify-content-start gap-2 mt-4">
                <button type="submit" class="btn btn-danger"><i class="fas fa-trash-alt me-2"></i>نعم, قم بالحذف</button>
                <a asp-action="Index" class="btn btn-secondary"><i class="fas fa-times me-2"></i>إلغاء</a>
            </div>
        </form>
    </div>
</div>