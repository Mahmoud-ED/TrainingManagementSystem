@model TrainingManagementSystem.Models.Entities.Qualification

@{
    ViewData["Title"] = "تعديل المؤهل: " + Model.Name;
}

<!-- Breadcrumbs -->
@* <div class="breadcrumbs overlay" style="background-image: url('@Url.Content("~/mediplus_theme/img/breadcrumb-bg.jpg")');">
    <div class="container">
        <div class="bread-inner">
            <div class="row">
                <div class="col-12">
                    <h2>@ViewData["Title"]</h2>
                    <ul class="bread-list">
                        <li><a asp-controller="Admin" asp-action="Index">لوحة التحكم</a></li>
                        <li><i class="icofont-simple-right"></i></li>
                        <li><a asp-controller="Qualifications" asp-action="Index">المؤهلات</a></li>
                        <li><i class="icofont-simple-right"></i></li>
                        <li class="active">تعديل مؤهل</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- End Breadcrumbs --> *@

<section class="appointment single-page section">
    <div class="container">
        <div class="row">
            <div class="col-lg-6 col-md-8 col-12 mx-auto">
                <div class="appointment-inner">
                    <div class="title text-center mb-4">
                        <h3><i class="icofont-edit"></i> تعديل بيانات المؤهل</h3>
                        <p>يرجى تحديث اسم المؤهل.</p>
                    </div>

                    <form asp-action="Edit" asp-route-id="@Model.Id" method="post" class="form">
                        @Html.AntiForgeryToken()
                        <input type="hidden" asp-for="Id" /> @* مهم جداً لتحديد أي سجل يتم تعديله *@
                        @* إذا كان لديك خصائص أخرى مثل CreatedAt, UserId لا تريد تعديلها، أضفها كـ hidden fields *@
                        @* <input type="hidden" asp-for="CreatedAt" /> *@
                        @* <input type="hidden" asp-for="UserId" /> *@


                        <div asp-validation-summary="ModelOnly" class="alert alert-danger text-center p-2 mb-3" role="alert"></div>

                        <div class="form-group mb-3">
                            <label asp-for="Name" class="form-label"></label>
                            <input asp-for="Name" class="form-control" placeholder="اسم المؤهل" />
                            <span asp-validation-for="Name" class="text-danger d-block mt-1 small"></span>
                        </div>


                        <div class="row mt-4">
                            <div class="col-12">
                                <div class="form-group">
                                    <div class="button d-flex justify-content-center gap-2">
                                        <a asp-action="Index" class="btn btn-secondary">
                                            <i class="icofont-close-line-circled"></i> إلغاء
                                        </a>
                                        <button type="submit" class="btn btn-primary">
                                            <i class="icofont-save"></i> حفظ التعديلات
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}